const Crawler = require('./crawler/crawler.class');
const CrawlerConfig = require('./crawler/crawler-config.class');
const Mongo = require('./mongo/mongo.class');
const MongoConfig = require('./mongo/mongo-config.class');

module.exports = {
  Crawler,
  CrawlerConfig,
  Mongo,
  MongoConfig
};